from setuptools import find_packages, setup


setup(
    name="jiter",
    version="0.12.0+refrator.1",
    description="Refrator Android compatibility shim for jiter",
    packages=find_packages(),
    python_requires=">=3.9",
    license="Apache-2.0",
)
