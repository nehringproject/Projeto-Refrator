from setuptools import find_packages, setup


setup(
    name="tokenizers",
    version="0.22.2+refrator.1",
    description="Refrator Android compatibility shim for tokenizers",
    packages=find_packages(),
    python_requires=">=3.9",
    license="Apache-2.0",
)
